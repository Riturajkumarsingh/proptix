{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/server.js"
    }
  ]
}
